# Allow install on Node 23 (many deps list ^18||^20||^22||>=24 and omit 23)
ignore-engines=true
# Only show errors (hides EBADENGINE; use `npm install --loglevel warn` for full output)
loglevel=error
